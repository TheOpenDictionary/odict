namespace schema;

enum POS:byte {
    verb = 0,
    noun,
    adj,
    pronoun,
    adv,
    prep,
    conj,
    intj,
    prefix,
    suffix,
    particle
}

table Etymology {
    description:string;
    usages:[Usage];
}

table Group {
    id:string;
    description:string;
    definitions:[string];
}

table Usage {
    pos:string;
    definitions:[string];
    groups:[Group];
}

table Entry {
    term:string (key);
    etymologies:[Etymology];
}

table Dictionary {
    entries:[Entry];
}

root_type Dictionary;