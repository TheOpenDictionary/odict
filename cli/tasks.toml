"clean:cli" = "cargo clean -p cli"

["build:cli"]
run = "cargo build -p cli"
alias = "build"
sources = ["cli/src/**/*.rs", "cli/Cargo.toml"]
outputs = { auto = true }

["run:cli"]
run = "cargo run -p cli"
alias = "run"
