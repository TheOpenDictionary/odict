"clean:cli" = "cargo clean -p cli"

["build:cli"]
run = "cargo build -p cli"
alias = "build"

["run:cli"]
run = "cargo run -p cli"
alias = "run"

["generate-ci:cli"]
run = "dist init -y"
description = "Generate GitHub Actions CI workflow for the Rust CLI"
