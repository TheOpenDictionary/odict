"publish:lib" = "cargo publish -p odict"
"build:lib" = "cargo build -p odict"
"clean:lib" = "cargo clean -p odict"
"test:lib" = "cargo test -p odict --all-features"
"bench:lib" = "cargo bench -p odict"
